//let emptiness // Todas as vezes que eu não atribuir um valor para variável e tentar usar ela, undefined é exibido automaticamente pelo JS


// Quando vou usar o null?
// Quando eu quiser indicar intencionalmente que não existe valor em uma variável

let emptiness = null

console.log(emptiness, emptiness + 3, `O valor é ${emptiness}`)