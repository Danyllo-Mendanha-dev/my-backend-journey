// const h1 = document.querySelector('body > h1') // Verifica só uma ocorrência

// console.log(h1)

const paragraphs = document.querySelectorAll('p') // Verifica várias ocorrências

const errors = document.querySelectorAll('.error')
console.log(errors)

// paragraphs.forEach(paragraph =>{ // Acesso múltiplos valores p
//   console.log(paragraph)
// })